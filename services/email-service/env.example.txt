# Configuracoes SMTP
# Suporta Gmail, Outlook, Mailgun, e outros servidores SMTP
# Copie este arquivo para .env e preencha com suas credenciais

# Servidor SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=tls

# Credenciais (email e senha ou senha de app)
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-senha-ou-senha-de-app

# Email remetente
EMAIL_FROM=seu-email@gmail.com
EMAIL_FROM_NAME=Sistema de Eventos

# Configuracoes do Banco de Dados (opcional, se diferente do padrao)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=eventos_db
DB_USER=postgres
DB_PASSWORD=postgres

